import React from "react";

const AutoComplete = ({
  onChange = () => {},
  onBlur = () => {},
  onFocus = () => {},
  fetchSuggestions = () => {},
  onSelect = () => {},
  placeHolder = "",
  customStyles,
  dataKey,
  customLoading,
  staticData,
}) => {
  return <div>AutoComplete</div>
};

export default AutoComplete;
